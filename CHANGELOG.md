# 更新日志

## 0.2.0 — 2026-09-09

- 新增：英文界面。菜单、设置页、工具栏提示、弹窗和保存文件名跟随系统语言：系统语言为英文时显示英文，其他语言显示中文；文案在 `Cropmark/Resources/Localizable.xcstrings`
- 新增：剪贴板同时附带 PNG 文件。同一个剪贴板项里带图片和文件引用，Finder、Slack、终端、Claude Code 这类只认文件的地方也能直接粘贴；文件放在 `~/Library/Caches/Cropmark/Clipboard/`，只留最近 20 张，可在设置里关闭
- 新增：Retina 截图可缩小到 1x，复制和保存分别设置；尺寸标签在 Retina 屏幕上标 `@2x` / `@1x`，数字始终是 Enter 复制出去的像素数
- 改进：设置页与 README 说明快捷键冲突的真实行为（同一快捷键多个应用都会响应，或被对方拦截），并说明 F1–F12 可以单独作为快捷键

## 0.1.1 — 2026-09-07

- 修复：选中马赛克工具时，粗细选项悬在工具栏外面、点不到
- 修复：抓屏尚未完成时再按快捷键会开出第二个会话，留下一层无法关闭的遮罩
- 修复：选区边落在半像素上时，导出图比屏幕显示的尺寸多 1 像素；选区现在吸附到像素网格，尺寸标签与导出用同一份换算
- 修复：文字标注在编辑框里折行、导出却是一行被裁掉；编辑中改颜色或粗细现在实时生效
- 构建：`make build` / `make test` 失败时不再静默返回成功
- 改进：截图完成或取消后，键盘焦点回到之前使用的应用，Enter 之后可直接 ⌘V
- 改进：导出的 PNG / TIFF 带 DPI 信息，Retina 截图粘贴到 Pages、邮件等应用不再显示成两倍大
- 改进：首次申请屏幕录制权限只弹系统授权框，不再叠一个自己的提示；授权后抓屏仍被系统拒绝时按权限问题引导
- 改进：冻结底图独立成一层只画一次，鼠标移动时不再整屏重绘全分辨率大图
- 改进：保存失败弹窗提示而不是只记日志；⌘S 保存前先复制到剪贴板，保存框取消或失败截图也不丢
- 改进：多屏时在旁观屏幕右键清除别处的选区而不是取消整个会话；把手拖成零尺寸后正确退回无选区状态
- 改进：光标随状态变化（把手拉伸、选区内移动、文字工具 I 形），未处理的按键不再响系统提示音，大写锁定下 ⌘Z 可用
- 修正文案：屏幕录制授权后需要退出并重新打开 Cropmark 才生效，设置页、提示框和 README 不再说「不需要重启」
- 改进：设置窗口从系统设置切回时刷新登录项状态，登录项需要手动批准时给出说明并跳转，改快捷键后标题说明同步更新，窗口只在首次打开时居中

## 0.1.0 — 2026-09-07

首个公开版本。

- 全局快捷键（默认 `⌃⌘A`，可改）唤起，菜单栏常驻
- 冻结所有屏幕画面，悬停自动识别窗口，单击选窗或拖选区域，实时显示像素尺寸
- 选区可拉伸、移动；工具栏提供矩形、椭圆、箭头、画笔、马赛克、文字，6 色 3 粗细，支持撤销
- `Enter` / 双击复制到剪贴板，`⌘S` 保存 PNG，`Esc` / 右键取消
- 设置：快捷键、提示音、登录时启动、屏幕录制权限状态
- 未公证：首次打开需在「系统设置 → 隐私与安全性」中点「仍要打开」
